[project]
name = "mcdonalds-optimizer"
version = "0.1.0"
description = "Optimize McDonald's menu choices."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "mip==1.17.6",
    "pandas==3.0.3",
    "streamlit==1.58.0",
]

[tool.uv]
package = false
