[project]
name = "mcdonalds-optimizer"
version = "0.1.0"
description = "Optimize McDonald's menu choices."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "mip==1.15.0",
    "pandas==2.2.1",
    "streamlit==1.54.0",
]

[tool.uv]
package = false
